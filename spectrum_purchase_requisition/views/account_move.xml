<odoo>
    <data>
        <record id="account_move_inherited" model="ir.ui.view">
            <field name="name">account.move.inherited</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='partner_id']" position="replace">
                    <field name="partner_id"  widget="res_partner_many2one" nolabel="1" context="{'res_partner_search_mode': (context.get('default_move_type', 'entry') in ('out_invoice', 'out_refund', 'out_receipt') and 'customer') or (context.get('default_move_type', 'entry') in ('in_invoice', 'in_refund', 'in_receipt') and 'supplier') or False,                                             'show_address': 1, 'default_is_company': True, 'show_vat': True}" domain="[('customer_status', '=', 'unblock'),('company_id', 'in', (False, company_id))]" options="{&quot;no_quick_create&quot;: True}" invisible="move_type not in ('out_invoice', 'out_refund', 'in_invoice', 'in_refund', 'out_receipt', 'in_receipt')" readonly="state != 'draft'"/>

                </xpath>
                <xpath expr="//button[@name='action_post'][2]" position="replace">
                    <button name="action_post" string="Confirm" class="oe_highlight" type="object"
                            groups="account.group_account_invoice" data-hotkey="q" context="{'validate_analytic': True}"
                            invisible=" move_type !='out_invoice' and state in ('draft','first_approval','second_approval','posted') or hide_post_button or move_type == 'entry' or display_inactive_currency_warning"/>
                </xpath>
                <xpath expr="//button[@name='button_cancel'][2]" position="replace">
                    <button name="button_cancel" string="Cancel" type="object" groups="account.group_account_invoice"
                            data-hotkey="x"
                            invisible="not id or state == 'posted' or move_type == 'entry'"/>
                </xpath>
                <xpath expr="//button[@name='action_post']" position="after">
                    <button name="validate_first_approval" string="Approval 1" invisible="state != 'draft' or move_type =='out_invoice'"
                            class="oe_highlight" type="object"/>
                    <button name="validate_second_approval" string="Approval 2" invisible="state != 'first_approval' or move_type =='out_invoice'"
                            class="oe_highlight" type="object"/>
                    <button name="validate_third_approval" string="Approval 3" invisible="state != 'second_approval' or move_type =='out_invoice'"
                            class="oe_highlight" type="object"/>
                </xpath>
            </field>
        </record>
    </data>
</odoo>